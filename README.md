简介:Backpacked 附属模组,为 Backpacked 背包界面添加可自定义翻页快捷键与页码常驻显示。

功能详述如下：

快捷键翻页 — 默认 Z=上一页、C=下一页,替代点击标题栏箭头按钮;

键位可自定义 — 选项 → 控制 → Backpacked 分类下与 B/V 键同组显示,可自由重绑

页码常驻显示 — 标题旁常驻 (当前页/总页)(如 (1/3))

游戏内配置 — NeoForge 配置界面,翻页间隔可调(1–1000ms,即时生效)

纯客户端模组，服务端不需要安装
